/** @type {import('next-sitemap').IConfig} */

module.exports = {
  siteUrl: 'https://tudigitalnegocio.com', // Siempre apunta a tu dominio de producción
  generateRobotsTxt: true, // Genera el archivo robots.txt
  sitemapSize: 5000, // Tamaño de las URLs por archivo de sitemap (puedes cambiarlo según tus necesidades)
  // Otras configuraciones si las necesitas
};

